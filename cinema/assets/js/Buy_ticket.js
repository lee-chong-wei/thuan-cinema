// This file is autogenerated via the `commonjs` Grunt task. You can require() this file in a CommonJS environment.

function pickMovie(){
  var x = document.getElementById("moviePick").value;
  var text = '';  
  for (let i = 0; i < rel.length; i++) {
    if (x == rel[i].movie_id) {
     var l = cinema[rel[i].cinema_id].name;
     text += "<option value=\'" + i + "\' selected>" + l + "</option>";
     document.getElementById("cinemaPick").innerHTML = text;
    }
  }  
}
  
  function pickDate(){
    var x = document.getElementById("moviePick").value;
    var d = new Date();
    var pick_date = '';
    for (let i = 0; i < 7; i++) {
      d.setDate(d.getDate()+1);
      var l = d.toDateString();
      pick_date += "<option value=\'" + i + "\' selected>" + l + "</option>";
      document.getElementById('datePick').innerHTML = pick_date;
    }
  }
  function pickTime(){
    var x = document.getElementById("moviePick").value;
    var pick_time = '';
    for (let i = 0; i < movie_schedule.length; i++) {
      if (x == movie_schedule[i].movie_id) {
        var l = movie_schedule[i].time + ", " + movie_schedule[i].description;
        pick_time += "<option value=\'" + i + "\' selected>" + l + "</option>";
        document.getElementById('timePick').innerHTML = pick_time;
      }
    }
  }